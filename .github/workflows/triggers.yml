name: DevInHouse-Triggers

on:
  issues:
    types:  [opened]

jobs:
  new-comment:
    runs-on:  ubuntu-latest
    steps:
      - name: Get issue id
        run: echo ${{github.event.issue.node_id}}
